{
  "name": "did-wallet-app",
  "version": "0.0.0",
  "description": "DID Wallet Application - Desktop and Chrome Extension",
  "private": true,
  "scripts": {
    "dev": "start \"Extension Dev\" cmd /k \"cd ext && pnpm dev\" && cd desktop && yarn dev",
    "dev:desktop": "cd desktop && yarn dev",
    "dev:ext": "cd ext && pnpm build",
    "dev:issuer": "cd issuer-web && pnpm dev",
    "dev:metaverse": "cd metaverse-web && pnpm dev",
    "dev:verifier": "cd verifier-web && pnpm dev",
    "build": "npm run build:desktop && npm run build:ext && npm run build:issuer && npm run build:metaverse",
    "build:desktop": "cd desktop && yarn build",
    "build:ext": "cd ext && pnpm build",
    "build:issuer": "cd issuer-web && pnpm build",
    "build:metaverse": "cd metaverse-web && pnpm build",
    "build:verifier": "cd verifier-web && pnpm build",
    "start": "npm run dev",
    "start:desktop": "npm run dev:desktop",
    "start:ext": "npm run dev:ext",
    "start:issuer": "npm run dev:issuer",
    "start:metaverse": "npm run dev:metaverse",
    "test": "npm run test:desktop && npm run test:ext && npm run test:issuer && npm run test:metaverse",
    "test:desktop": "echo \"Desktop tests not configured\"",
    "test:ext": "cd ext && pnpm e2e"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
