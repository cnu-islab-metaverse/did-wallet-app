# 아카이브 — 초기 지갑 앱 소스 (2026-07 스냅샷)

이 폴더는 UI/UX 재구축 **이전의 원본 지갑 앱 소스**(`src/`) 스냅샷이다. 개발자의 원래 의도·동작이
담긴 참조본으로, **빌드/실행에 연결되지 않는다**(어떤 프로젝트도 이 폴더를 import 하지 않음).

- 원본 위치: 리포 루트 `src/` (라이브는 계속 사용/재구축됨)
- 스냅샷 시점: 2026-07 (메타마스크 기반 UI, `App.tsx` 단일 파일 중심)
- 제외: `node_modules/`, `yarn.lock`
- 목적: 재구축 중 "원래 이렇게 동작/의도했다"를 대조하는 레퍼런스. git 이력에도 동일 내용이 있으나
  브라우징 편의를 위해 명시적으로 보존.

재구축 계획: `wallet/wallet-ui-rebuild-plan.md` 참조.
